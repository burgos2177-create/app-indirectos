// Sello de versión desplegada. Se muestra en la barra superior para poder
// verificar de un vistazo si el navegador ya está corriendo el último deploy
// (la app son módulos ES servidos por GitHub Pages: si el sello no cambia,
// es caché del navegador, no un cambio que falte).
//
// Convención: AAAA.MM.DD.n — se sube en cada despliegue.
export const APP_VERSION = '2026.08.02.1';
